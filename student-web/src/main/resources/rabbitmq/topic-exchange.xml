<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans    
                        http://www.springframework.org/schema/beans/spring-beans-3.1.xsd    
				        http://www.springframework.org/schema/rabbit 
				        http://www.springframework.org/schema/rabbit/spring-rabbit-1.4.xsd">

	<!-- direct：转发消息到 routigKey 指定的队列 -->
	<!-- topic：按规则转发消息（最灵活）。 -->
	<!-- fanout：转发消息到所有绑定队列 -->
	<!-- 定义默认的 -->
	<!-- 定义direct exchange，绑定queueTest -->
	<rabbit:direct-exchange name="defaultTopicExchange" durable="true" auto-delete="false">
		<rabbit:bindings>
			<rabbit:binding queue="defaultTopicQueueTest" key="defaultTopicQueueTestKey" />
		</rabbit:bindings>
	</rabbit:direct-exchange>

	<rabbit:topic-exchange name="topicExchange" durable="true" auto-delete="false">
		<rabbit:bindings>
			<rabbit:binding queue="topicQueueTest" pattern="topicQueueTestKey" />
		</rabbit:bindings>
	</rabbit:topic-exchange>
</beans>  